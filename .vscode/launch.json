{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Download and run the current file",
            "type": "ev3devBrowser",
            "request": "launch",
            "program": "/home/robot/${workspaceFolderBasename}/${relativeFile}"
        }
    ]
}
